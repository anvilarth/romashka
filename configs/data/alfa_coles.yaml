_target_: src.data.AlfaPretrainingDataModule


splitter:
  _target_: ptls.frames.coles.split_strategy.SampleSlices
  split_count: 5
  cnt_min: 20
  cnt_max: 60
  is_sorted: True

mode: 'coles'

data_dir: ${paths.data_dir}
batch_size: 32
min_seq_len: 0
max_seq_len: 250
num_workers: 10
pin_memory: True
seed: 42
shuffle: True